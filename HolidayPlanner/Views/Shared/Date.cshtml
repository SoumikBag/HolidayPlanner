@model System.Nullable<System.DateTime>


Using Date Template

@if (Model.HasValue)
{

    @Html.TextBox("", string.Format("{0:d}", Model.Value),
    new { @class = "datefield", type = "datetime" })
}